config = {
    "UPDATE_CHECK_DELAY": 600,
    "MONGODB_URL": "mongodb://localhost:27017",
    "ALLOW_SNAPSHOTS": True, # Decide latest release should be a snapshot or a full release.
    "POPULATE_ERROR_THRESHOLD": 500 # Maximum amount of recipes required for db population to have failed 
}